name: GitHub Metrics
on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    steps:
      # Block 1: Top Left (Base Stats)
      - name: Top Left - Base Stats
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.README }}
          filename: "metrics-top-left.svg"
          user: "Talal-Ali"
          template: "classic"
          base: "header, activity, community, repositories, metadata"
          config_timezone: "Asia/Karachi"

      # Block 2: Top Right (3D Graph & Habits)
      - name: Top Right - Graph & Habits
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.README }}
          filename: "metrics-top-right.svg"
          user: "Talal-Ali"
          template: "classic"
          base: ""
          config_timezone: "Asia/Karachi"
          
          plugin_isocalendar: "yes"
          plugin_isocalendar_duration: "half-year"
          
          plugin_habits: "yes"
          plugin_habits_days: 14
          plugin_habits_charts: "yes"
          plugin_habits_facts: "no"
          
          plugin_stargazers: "yes"

      # Block 3: Bottom Left (Languages & LeetCode)
      - name: Bottom Left - Languages & LeetCode
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.README }}
          filename: "metrics-bottom-left.svg"
          user: "Talal-Ali"
          template: "classic"
          base: ""
          config_timezone: "Asia/Karachi"
          
          plugin_languages: "yes"
          plugin_languages_details: "bytes-size, percentage"
          plugin_languages_sections: "most-used"
          plugin_languages_limit: 8
          
          plugin_leetcode: "yes"
          plugin_leetcode_user: "_talal_ali_"
          plugin_leetcode_sections: "status, ranking"

      # Block 4: Bottom Right (AniList)
      - name: Bottom Right - AniList
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.README }}
          filename: "metrics-bottom-right.svg"
          user: "Talal-Ali"
          template: "classic"
          base: ""
          
          plugin_anilist: "yes"
          plugin_anilist_user: "TalalAl1"
          plugin_anilist_medias: "anime"
          plugin_anilist_sections: "favorites"
          plugin_anilist_limit: 2
